name: Auto-publish Dev.to articles

on:
  schedule:
    - cron: "0 9 * * *"   # 09:00 UTC every day
  workflow_dispatch: {}

jobs:
  autopublish:
    runs-on: ubuntu-latest

    env:
      MAX_ARTICLES_PER_RUN: "3"

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install requests

      - name: Publish to Dev.to
        env:
          DEVTO_API_KEY: ${{ secrets.DEVTO_API_KEY }}
          MAX_ARTICLES_PER_RUN: ${{ env.MAX_ARTICLES_PER_RUN }}
        run: |
          python devto_publisher.py

      - name: Commit updated JSON files (if changed)
        run: |
          if git status --porcelain | grep -E "articles.json|articles_published.json"; then
            git config user.name "github-actions[bot]"
            git config user.email "github-actions[bot]@users.noreply.github.com"
            git add articles.json articles_published.json
            git commit -m "Update article queue after Dev.to autopublish"
            git push
          else
            echo "No JSON changes to commit."
          fi
